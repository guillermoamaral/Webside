# Create a new profiler

**URL**: `/profilers`

**Method**: `POST`

**Payload**:

```json
{
	"expression": "string",
	"context": "context"
}
```

Where `context` can be one of the listed [here](../evaluations/post.md).
